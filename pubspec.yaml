name: matches
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.1.0-143.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  http: ^1.0.0
  table_calendar: ^3.0.9
  flutter_bloc: ^8.1.3
  provider: ^6.0.5
  equatable: ^2.0.5
  intl: ^0.18.1
  scrollable_positioned_list: ^0.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
flutter:
  uses-material-design: true
  assets:
    - lib/resources/images/ball.png
